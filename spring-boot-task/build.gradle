dependencies{

}

jar {
    manifest {
        attributes("Main-Class": "com.cacoota.business.SpringBootExampleTaskApplication")
        attributes("Class-Path": configurations.compile.collect { "lib/$it.name" }.join(' '))
    }
}